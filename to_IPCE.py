import pandas as pd

def table_dev(path: str) -> pd.DataFrame["time", "Wavelength", "Photocurrent", "Shutter"]:
    pass

def graphical_dev(df: pd.DataFrame["time", "Wavelength", "Photocurrent", "Shutter"]) -> pd.DataFrame["Wavelength", "Photocurrent"]:
    pass