import streamlit as st

st.set_page_config(
    page_title="Анализ лабораторных данных",
    page_icon="🔬",
    layout="wide",
    initial_sidebar_state="expanded",
    menu_items={
        'Get Help': 'https://github.com/dagan3d/ipce/issues',
        'Report a bug': "https://github.com/dagan3d/ipce/issues",
        'About': "Приложение для обработки данных, полученных с лабораторного оборудования НИЛ ТНМ."
    }
)


st.title("Анализ лабораторных данных (НИЛ ТНМ)")

st.markdown("""
Этот сервис предназначен для автоматизации обработки данных, полученных с лабораторного оборудования НИЛ ТНМ.
Он позволяет выполнять предобработку, визуализацию и базовый анализ данных, экономя время и усилия исследователей.
""")

st.subheader("Доступные модули:")

# Используем колонки для лучшего расположения кнопок
col1, col2, col3, col4 = st.columns(4)

with col1:
    if st.button("IPCE", type="primary"):
        st.switch_page("pages/IPCE.py")
    """
    Обработка данных зависимости фототока от длины волны. Расчет квантовой эффективности (IPCE).
    """

with col1:
    if st.button("IPCE (NEW)", type="secondary"):  # Добавили кнопку для нового режима IPCE
        st.switch_page("pages/IPCE (NEW).py")
    """
    Обработка данных зависимости фототока от времени при разных длинах волн. Расчет IPCE.
    """

with col2:
    if st.button("Фототоки", type="primary"):
        st.switch_page("pages/Photocurrent.py")
    """
    Анализ данных зависимости фототока от времени.
    """

with col2:
    if st.button("Germanium", type="secondary"):  # Добавили кнопку для нового режима IPCE
        st.switch_page("pages/Germanium.py")
    """
    Анализ данных, полученных с германиевого детектора.
    """

with col3:
    if st.button("Оптическая ширина запрещённой зоны", type="primary"):
        st.switch_page("pages/OpticEg.py")
    """
    Обработка данных UV-Vis спектрофотометрии. Определение ширины запрещенной зоны.
    """

with col3:
    if st.button("Оптическое поглощение", type="secondary"):
        st.switch_page("pages/OpticAbsorption.py")
    """
    Анализ спектров оптического поглощения (UV-Vis).
    """


with col4:
    if st.button("SERS", type="primary"):
        st.switch_page("pages/SERS.py")
    """
    Анализ спектров комбинационного рассеяния (SERS).
    """


st.divider()

st.subheader("Рекомендации по использованию")
st.markdown("""
- **Необходимость стандартной обработки:** Используйте сервис, если требуется провести стандартную обработку большого количества файлов.
- **Оперативный просмотр данных:** Подходит для быстрого просмотра и предварительной оценки полученных данных.
- **Финальная обработка:**  Сервис *не предназначен* для финальной обработки графиков.  Для подготовки публикаций используйте специализированное ПО (Origin, QtiPlot и т.д.).
""")

st.subheader("Поддерживаемые форматы файлов")
st.markdown("""
- **.txt:** Текстовые файлы с разделителями (табуляция, пробелы).
- **.csv:** Файлы, разделенные запятыми (или точкой с запятой).
- **.xls:** Файлы Excel (для некоторых модулей).
- **.pts:** Файлы, полученные с приборов НИЛ ТНМ (для некоторых модулей).

**Важно:** Структура данных в файлах должна соответствовать требованиям конкретного модуля (см. документацию к модулю).
""")


st.divider()

st.subheader("Обратная связь и контакты")
st.markdown("""
По вопросам решения проблем, предложений по улучшению и модификации обращайтесь:
""")
st.page_link("https://t.me/Danil_Butmanov",
             label="Telegram: Данил Бутманов", icon="✉️")
# st.markdown("[Telegram: Данил Бутманов](https://t.me/Danil_Butmanov)") # Альтернативный вариант (если page_link не работает)
